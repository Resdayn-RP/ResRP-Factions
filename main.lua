if not ResdaynRP then return end

---@class factions
local factions = {}

factions.config, factions.tableConfig = require "custom.ResRP-Factions.config"
factions.functions = require "custom.ResRP-Factions.functions"

return factions